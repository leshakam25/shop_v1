import HeaderWrapper from "@/shared/components/TheHeader/HeaderWrapper";

const TheHeader = () => {
    return (
        <header>
           <HeaderWrapper/>
        </header>
    );
};

export default TheHeader;