import HeaderWrapper from "@/components/TheHeader/HeaderWrapper";

const TheHeader = () => {
    return (
        <header>
           <HeaderWrapper/>
        </header>
    );
};

export default TheHeader;